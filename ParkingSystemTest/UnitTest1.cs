namespace MainApp.Tests
{
    [TestClass]
    public class ProgramTests
    {
        [TestMethod]
        public void GetRequiredSpots_Car_ReturnsOne()
        {
            //string vehicleType = "car";
            //double result = ParkingManager.GetRequiredSpots(vehicleType);
            //Assert.AreEqual(1, result);
        }

        [TestMethod]
        public void CalculateParkingCost_15MinutesCar_Returns10()
        {
            //TimeSpan duration = TimeSpan.FromMinutes(15);
            //string vehicleType = "car";
            //int result = ParkingManager.CalculateParkingCost(duration, vehicleType);
            //Assert.AreEqual(10, result); // 15 minutes - 10 free minutes = 5 minutes * 2 CZK = 10 CZK
        }
    }
}